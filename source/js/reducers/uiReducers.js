import { STORY_MODE } from 'constants'
// Handle App Mode switching & sidebar filter actions

export function mode(state=STORY_MODE , action) {
  return state
}

export function sidebarFilter(state=undefined, action) {
  return state
}
